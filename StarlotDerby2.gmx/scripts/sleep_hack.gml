//Sleep hack code I found online. 

var t = current_time + argument0; 
while (current_time < t) 
{
if (instance_number(ball) > 0) {
ball.image_angle+= manager.ballSpeed*3;
}
    //Any extra effects can be played here. 
}
